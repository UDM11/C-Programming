#include<stdio.h>
main()
{
    int len, bre, result;
    result=len*bre;
    printf("Enter the lenght of Rectangular: ");
    scanf("%d", &len);
    printf("Enter the bredth of Rectangular: ");
    scanf("%d", &bre);
    printf("The area of Rectangular is %d", &result);
}
