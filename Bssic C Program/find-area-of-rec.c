#include<stdio.h>
int main()
{
    int len, bre;
    printf("Enter the lenght of Rectangular: ");
    scanf("%d", &len);
    printf("Enter the bredth of Rectangular: ");
    scanf("%d", &bre);
    printf("The area of Rectangular is %d", len*bre);
    return 0;
}